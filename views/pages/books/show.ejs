<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
   <link rel="stylesheet" href="./public/style.css">
</head>
<body>
    <header>
        <h2>Find Your Favourit Book</h2>
        <Button><a href="/">Home</a></Button>
        <Button><a href="/searches"></a> Search Your Book</a></Button>
    </header>

    <main>
        <section>
            <% dataBaseData.forEach((item)=>{ %> 
                <div class="books">
                   <img src="<%= item.imageLinks %>" alt="">
                   <h3><%= item.title %></h3>
                   <h4><%= item.authors %></h4>
                   <p><%= item.description %></p>
               </div>
              <% }) %>
        </section>
    </main>
</body>
</html>